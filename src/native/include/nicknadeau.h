#ifndef __NICK_NADEAU_H__
#define __NICK_NADEAU_H__

// De-clutter includes from the source files, so that they are cleaner when displayed on the web.
#include <stdio.h>
#include <string.h>

#define LINK(func) void func();

#endif	//__NICK_NADEAU_H__
